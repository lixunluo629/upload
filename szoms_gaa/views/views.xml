<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="zabbix.list">
      <field name="name">zabbix list</field>
      <field name="model">zabbix.zabbix</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="zabbix.action_window">
      <field name="name">zabbix window</field>
      <field name="res_model">zabbix.zabbix</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="zabbix.action_server">
      <field name="name">zabbix server</field>
      <field name="model_id" ref="model_zabbix_zabbix"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="zabbix" id="zabbix.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="zabbix.menu_1" parent="zabbix.menu_root"/>
    <menuitem name="Menu 2" id="zabbix.menu_2" parent="zabbix.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="zabbix.menu_1_list" parent="zabbix.menu_1"
              action="zabbix.action_window"/>
    <menuitem name="Server to list" id="zabbix" parent="zabbix.menu_2"
              action="zabbix.action_server"/>
    -->
  </data>
</odoo>